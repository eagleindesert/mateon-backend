# JaCoCo 가 getter/setter/생성자를 미커버로 세지 않게, Lombok 이 만든 멤버에
# @lombok.Generated 를 붙인다. JaCoCo 0.8.2+ 가 이 어노테이션을 무시한다.
# 이 파일이 없으면 커버리지 숫자가 Lombok 보일러플레이트에 깎인다.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
